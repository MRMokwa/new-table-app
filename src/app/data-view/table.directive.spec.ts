import { TableDirective } from './view/table.directive';

describe('TableDirective', () => {
  it('should create an instance', () => {
    const directive = new TableDirective();
    expect(directive).toBeTruthy();
  });
});
